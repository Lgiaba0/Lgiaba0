#include <stdio.h>
#include <math.h>
int main () {
    int a;
    scanf ("%d", &a);
    if ( a == 0) printf ("%d la so 0", a);
    else if ( a > 0) printf ("%d la so nguyen duong", a);
    else printf ("%d la so nguyen am", a); 
}
