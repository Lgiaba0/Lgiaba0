#include <stdio.h>
#include <math.h>
int main () {
	int a;
	scanf ("%d", &a);
	if ( a == 100) {
		printf ("%d bang 100", a);
	}
	else if ( a > 100 ) {
		printf ("%d lon hon 100", a);
	}
	else printf ("%d be hon 100", a);
}
